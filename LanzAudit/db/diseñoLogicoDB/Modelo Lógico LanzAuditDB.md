users(**id**, username, email, password_hash, profile_picture, role, created_at)

scans(**id**, *user_id*, scan_type, scan_parameters, status, created_at)

scan_results(**id**, *scan_id*, result, created_at)
